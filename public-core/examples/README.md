# Example data upload status

The frozen RC3.9 learner public core contains two canonical example tables:

- `KKY_RC3_9_EXAMPLE_BANK.csv` — 177 rows
- `KKY_RC3_9_NEGATIVE_APPENDIX.csv` — 63 rows

Their frozen copies are present in the Stage20-012A learner public-core ZIP and in the Stage20-014 publication handoff package. They still need to be committed to this directory or attached to the `rc3.9` GitHub Release. Do not regenerate or silently edit them; verify the SHA-256 values against `SHA256SUMS_ORIGINAL_RELEASE.txt`.
